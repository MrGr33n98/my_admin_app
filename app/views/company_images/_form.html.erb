<%= form_with(model: company_image) do |form| %>
  <% if company_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(company_image.errors.count, "error") %> prohibited this company_image from being saved:</h2>

      <ul>
        <% company_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :company_id, style: "display: block" %>
    <%= form.text_field :company_id %>
  </div>

  <div>
    <%= form.label :image_type, style: "display: block" %>
    <%= form.text_field :image_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
