<p style="color: green"><%= notice %></p>

<h1>Lead saas</h1>

<div id="lead_saas">
  <% @lead_saas.each do |lead_saa| %>
    <%= render lead_saa %>
    <p>
      <%= link_to "Show this lead saa", lead_saa %>
    </p>
  <% end %>
</div>

<%= link_to "New lead saa", new_lead_saa_path %>
