'use client'
import React, { FC } from 'react'

interface ContactTabProps { info?: any }

const ContactTab: FC<ContactTabProps> = ({ info }) => (
  <div className="tab-content" id="contact-tab">
    {/* cole aqui seu formulário/contato */}
  </div>
)

export default ContactTab
